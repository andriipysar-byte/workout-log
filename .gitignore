# macOS
.DS_Store
.localized
._*
.Spotlight-V100
.DocumentRevisions-V100
.TemporaryItems
.Trashes
.fseventsd
.apdisk

# Rust
/target/
**/target/

# Dioxus build output (`dx serve` / `dx build`)
/dist/
**/dist/
.dioxus/

# Node is only the Tailwind CLI. The generated assets/tailwind.css is committed
# on purpose, so a plain `cargo build` needs no Node at all.
node_modules/

# Generated heatmap exports
out/
