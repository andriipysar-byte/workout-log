name: workout_log_core
description: Domain core for WorkoutLog — models, notation parsing, storage and analytics.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  collection: ^1.19.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
